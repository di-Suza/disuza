# Widgets Layer

`widgets` is reserved for large reusable page sections that combine multiple features or entities.

Examples that may move here later:

- feed/sidebar composition
- dashboard portfolio preview sections
- notification/action panels
- future chat room shell

Do not move a component here only because it is visually large. Move it when multiple pages need the same composed product section.
