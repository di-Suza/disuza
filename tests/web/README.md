# Web Tests

Future web tests should cover page hooks, modal workflows, cache updates, error states, empty states, and responsive rendering for v1-parity surfaces.

Server state belongs to RTK Query in tests just like it does in the app.
