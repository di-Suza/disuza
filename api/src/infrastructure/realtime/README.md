# Realtime Adapter

Socket.IO is active for realtime product flows.

Socket startup, handshake auth, room membership checks, event schemas, presence, collab room events, chat fan-out, post-upload progress, typing indicators, notification fan-out, and audio-call signaling live behind this boundary. Add the Socket.IO Redis adapter here before running multiple API instances.
