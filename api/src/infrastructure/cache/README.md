# Cache Adapter

Redis is planned but not active in v2 yet.

When added, keep Redis clients, cache key builders, distributed locks, and pub/sub connections here. Domain services should ask for product operations instead of using raw Redis clients directly.
